# ---------------------------------------------------------------------------
# Production-profile overrides
# ---------------------------------------------------------------------------
# APP_STORAGE_ENCRYPTION_KEY is required. The application will refuse to
# start if this environment variable is absent or blank.
# ---------------------------------------------------------------------------

app.storage.encryption-key=${APP_STORAGE_ENCRYPTION_KEY}
