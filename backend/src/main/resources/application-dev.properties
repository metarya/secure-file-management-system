# ---------------------------------------------------------------------------
# Development-profile overrides
# ---------------------------------------------------------------------------
# WARNING: The encryption key below is a well-known development default.
# It is committed to source control and MUST NEVER be used in production.
# In production, set the APP_STORAGE_ENCRYPTION_KEY environment variable.
# ---------------------------------------------------------------------------

app.storage.encryption-key=${APP_STORAGE_ENCRYPTION_KEY:dev-storage-encryption-key-change-me}
